[user]
    name = {{ .name | quote }}
    email = {{ .email | quote }}
    signingkey = {{ .gpg_sign_key_id }}

[core]
    editor = {{ .editor }}
    excludesfile = ~/.gitignore_global
    pager = less -r
[pull]
    rebase = true

