[user]
    name = {{ .name | quote }}
    email = {{ .email | quote }}
{{- if .personal_secrets }}
    signingkey = {{ .gpg_key_id }}
{{- end }}

[core]
    editor = {{ .editor }}
    excludesfile = ~/.gitignore_global
[pull]
    rebase = true

